<div>
    <form action="#">
        <button class="icon">
            <img src="./search-icon.svg" />
        </button>

        <input placeholder="Search your issue..." />

        <button type="button" class="icon">
            <img src="./clear.svg" />
        </button>

        <button type="submit" class="submit">Search</button>
    </form>

    <div class="line">
        <div class="pills">
            <button class="white">FAQs</button>
            <button class="purple">Common Issues</button>
        </div>

        <p>Use keywords for best result</p>
    </div>
</div>

<style>
    form {
        width: 60vw;
        height: 70px;
        margin-top: 3rem;
        background-color: var(--primary-bg-color);
        box-shadow: 0px 5px 25px rgba(0, 0, 0, 0.05);
        border-radius: 4px;
        display: flex;
        padding: 0 20px;
        align-items: center;
    }

    form img {
        width: 100%;
        height: 100%;
    }

    form button.icon {
        width: 20px;
        height: 20px;
        border: none;
        padding: 0;
        background-color: transparent;
        cursor: pointer;
        margin-right: 10px;
    }

    form input {
        background-color: inherit;
        border: none;
        font-weight: 400;
        font-size: 20px;
        line-height: 24px;
        outline: none;
        flex-grow: 2;
    }

    form button.submit {
        background-color: var(--purple-shade);
        border-radius: 4px;
        border: none;
        padding: 10px 20px;
        color: var(--primary-bg-color);
        font-family: inherit;
        font-weight: 500;
        font-size: 20px;
        line-height: 24px;
        cursor: pointer;
        margin-left: 10px;
    }

    .pills {
        margin-top: 20px;
    }

    .pills button {
        border-radius: 21px;
        border: none;
        font-size: 16px;
        line-height: 19px;
        padding: 5px 10px;
        margin-right: 5px;
        cursor: pointer;
    }

    .pills button.white {
        background-color: var(--primary-bg-color);
        color: var(--purple-shade);
    }

    .pills button.purple {
        background-color: var(--purple-shade);
        color: var(--primary-bg-color);
    }

    .line {
        display: flex;
        justify-content: space-between;
    }

    .line p {
        font-weight: 300;
        font-size: 14px;
        line-height: 16px;
        color: rgba(57, 82, 145, 0.7);
        text-align: center;
    }
</style>
